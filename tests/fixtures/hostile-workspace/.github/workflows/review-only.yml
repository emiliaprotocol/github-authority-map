name: Review-only authority paths

on:
  pull_request_target:

permissions: read-all

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@0123456789abcdef0123456789abcdef01234567
        with:
          ref: ${{ github.event.pull_request.base.sha }}
      - run: echo "trusted base only"
